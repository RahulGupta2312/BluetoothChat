<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.forwardcode.bluetoothmessenger"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.BLUETOOTH"/>
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="Bluetooth Messenger"
        android:theme="@style/AppBaseTheme" >
        <activity
            android:name="in.forwardcode.bluetoothmessenger.SplashScreen"
            android:theme="@style/SplashScreenTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
           
        </activity>
        <activity
            android:name="in.forwardcode.bluetoothmessenger.DeviceListActivity"
            android:excludeFromRecents="true"
            >
            <!-- android:theme="@style/DeviceListActivity" -->
            </activity>
        <activity android:name="in.forwardcode.bluetoothmessenger.ChatActivity">
             
        </activity>
           
       
    </application>

</manifest>
